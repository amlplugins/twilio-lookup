/**
 * @amlplugins/twilio-lookup
 *
 * Thin re-export of twilio (CommonJS `export =` SDK).
 *
 * Twilio Lookup — phone number validation, carrier lookup, line-type intelligence, identity match.
 */

import _sdk from "twilio";
export { _sdk as sdk };
export default _sdk;
